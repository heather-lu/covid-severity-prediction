# Data

The analysis expects `severity.RData` in this folder. Loading the file creates a data frame named `dat` with 10,000 participants and 15 variables.

The dataset is not tracked in this repository. Place your copy here before knitting `analysis/covid_severity.Rmd`.
